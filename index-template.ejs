<!-- filepath: /Users/victor.elgersma41/dev/personal/FitnessTracker/index-template.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fitness Tracker</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-black text-white font-mono min-h-screen flex items-center justify-center p-8">
    <div class="container mx-auto max-w-md border-4 border-white p-8 text-center shadow-[8px_8px_0_0_rgba(255,255,255,1)] rounded-lg bg-black">
        <h1 class="text-4xl font-bold mb-10 uppercase tracking-wider border-b-4 border-white pb-6">Fitness Tracker</h1>
        <div class="space-y-8">
            <% configs.forEach(cfg => { %>
                <a href="<%= cfg.filename %>"
                   class="block w-full bg-<%= cfg.color %> text-black font-bold py-6 px-4 border-4 border-<%= cfg.color %> hover:bg-black hover:text-<%= cfg.color %> hover:border-<%= cfg.color %> transition-colors duration-150 text-2xl uppercase tracking-wider rounded shadow-[4px_4px_0_0_rgba(0,0,0,0.1)]">
                   <%= cfg.title %><% if(cfg.unitLabel !== 'meters' && cfg.unitLabel !== 'km') { %>s<% } %>
                </a>
            <% }) %>
        </div>
        <div class="mt-10">
            <a href="https://github.com/victorelgersma/pushups-2"
               class="inline-block bg-white text-black font-bold py-2 px-4 border-2 border-black rounded hover:bg-black hover:text-white transition-colors duration-150 uppercase tracking-wider shadow-[2px_2px_0_0_rgba(0,0,0,1)]"
               target="_blank" rel="noopener">
                GitHub Repo
            </a>
        </div>
    </div>
</body>
</html>
